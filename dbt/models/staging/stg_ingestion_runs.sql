-- Keep all statuses for eligibility joins and operational health.
select
    run_id,
    request_id,
    source,
    product,
    requested_date,
    started_at,
    status,
    commit_completed,
    package_version,
    library_version,
    warehouse_schema,
    logical_key_schema,
    content_hash_schema,
    requested_location,
    completed_at,
    knowledge_at,
    rows_fetched,
    rows_normalized,
    rows_accepted,
    rows_rejected,
    new_contents,
    new_transitions,
    batch_hash,
    error_class,
    error_message,
    commit_job_id,
    commit_sequence,
    query_bytes_processed,
    query_bytes_billed,
    code_revision
from {{ source('raw', 'ingestion_runs') }}
